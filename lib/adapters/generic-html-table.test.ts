import { describe,expect,it } from "vitest";
import { parseGenericHtmlTable } from "./generic-html-table";
import type { SourceDefinition } from "../types";
const source:SourceDefinition={id:"test",prefecture:"滋賀",title:"公式",indexUrl:"https://example.jp/official",format:"html",adapter:"generic-html-table"};
describe("generic html table",()=>{it("parses dated rows without promoting a broad area to exact",()=>{const rows=parseGenericHtmlTable("<h1>令和8年9月</h1><table><tr><td>9月24日</td><td>木</td><td>高島市内</td><td>速度違反</td><td>午後</td><td>高島署</td></tr></table>",source);expect(rows).toHaveLength(1);expect(rows[0]).toMatchObject({date:"2026-09-24",location:"高島市内",locationPrecision:"area_only",category:"速度違反"});});it("supports numeric day cells and qualifies generic areas",()=>{const rows=parseGenericHtmlTable("<h1>交通取締情報令和8年9月</h1><table><tr><td>1</td><td>火曜日</td><td>午前</td><td>小林</td><td>市内</td><td>各路線</td><td>交差点違反</td></tr></table>",{...source,prefecture:"宮崎"});expect(rows[0]).toMatchObject({date:"2026-09-01",location:"小林市内",locationPrecision:"area_only"});});it("carries a rowspan date across following rows",()=>{const html='<h1>令和8年9月</h1><table><tr><th>日</th><th>曜日</th><th>時間帯</th><th>路線</th><th>場所</th><th>取締種別</th></tr><tr><td>21</td><td>月</td><td>午前</td><td>県道</td><td>熊本市中央区水前寺公園</td><td>自転車</td></tr><tr><td>午前</td><td>国道325</td><td>阿蘇郡南阿蘇村白川</td><td>速度</td></tr></table>';const rows=parseGenericHtmlTable(html,{...source,prefecture:"熊本"});expect(rows).toHaveLength(2);expect(rows[1]).toMatchObject({date:"2026-09-21",route:"国道325",category:"速度"});});});
