import{describe,expect,it}from"vitest";import{cloudPayload}from"./cloud-api";import type{EnforcementRecord}from"./types";
const record=(id:string,activeTo:string|null):EnforcementRecord=>({id,prefecture:"愛媛",date:"2026-09-25",timeLabel:null,startTime:null,endTime:null,location:"国道11号",route:"国道11号",category:"速度",policeStation:null,latitude:null,longitude:null,locationPrecision:"area_only",sourceUrl:"https://www.police.pref.ehime.jp/",sourceTitle:"公式",status:"confirmed",reviewReason:null,rawText:"x",activeFrom:"2026-09-25",activeTo});
describe("cloud payload",()=>{it("expires records whose official validity ended before today",()=>{const payload=cloudPayload([record("old","2026-09-24"),record("today","2026-09-25"),record("policy",null)],new Date("2026-09-25T03:00:00Z"));expect(payload.records.map(r=>r.id)).toEqual(["today","policy"]);expect(payload.schemaVersion).toBe(1);});it("marks a daily deployment stale after 36 hours",()=>{const payload=cloudPayload([],new Date("2026-09-25T00:00:00Z"));expect(payload.staleAfter).toBe("2026-09-26T12:00:00.000Z");});});
