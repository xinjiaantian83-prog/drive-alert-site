import type {MetadataRoute} from "next";import {getRegionStatuses} from "@/lib/region-status";
export const dynamic="force-static";
export default function sitemap():MetadataRoute.Sitemap{const base="https://drive-alert.jp",now=new Date();return [{url:base,lastModified:now,priority:1},{url:`${base}/regions`,lastModified:now,priority:.9},{url:`${base}/sources`,lastModified:now,priority:.9},{url:`${base}/reliability`,lastModified:now,priority:.8},{url:`${base}/privacy`,lastModified:now,priority:.5},{url:`${base}/support`,lastModified:now,priority:.7},...getRegionStatuses().map(r=>({url:`${base}/regions/${r.slug}`,lastModified:now,priority:.6}))]}
